# 🔍 PDF Parser Application с OCR поддержкой

## 📋 Описание

PDF Parser Application - это мощное приложение для парсинга и анализа PDF документов с **встроенной поддержкой OCR (Optical Character Recognition)**. Приложение автоматически определяет качество извлеченного текста и при необходимости использует OCR для улучшения результатов.

## ✨ Основные возможности

### 🔍 **OCR функциональность**
- **Автоматическое улучшение качества** - OCR включается только при необходимости
- **Поддержка русского и английского языков** - оптимизировано для ваших документов
- **Умный fallback** - стандартное извлечение + OCR улучшение
- **Определение типа документа** - автоматическая классификация счетов, предложений и т.д.
- **Валидация качества** - оценка извлеченных данных

### 📊 **Парсинг документов**
- Универсальный парсер для всех типов документов
- Коммерческие предложения
- Счета на оплату
- Конкурсная документация
- Автоматическое извлечение таблиц

### 🎯 **Анализ и сопоставление**
- Сопоставление товаров с базой данных
- Fuzzy matching с настраиваемыми порогами
- Профили поставщиков
- Экспорт в Excel

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/katyLiminche/pdf_parser_app.git
cd pdf_parser_app
```

### 2. Создание виртуального окружения
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

## 🔧 Использование

### Запуск GUI приложения
```bash
python app/main.py
```

### Использование OCR в коде
```python
from app.pipeline.universal_parser import UniversalParser

# Создание парсера с OCR
parser = UniversalParser(use_ocr=True, ocr_languages=['ru', 'en'])

# Парсинг PDF файла с OCR
result = parser.parse_pdf_file("document.pdf", enable_ocr=True)

# Проверка статуса OCR
status = parser.get_ocr_status()
print(f"OCR статус: {status['status']}")
```

### Прямое использование OCR
```python
from app.pipeline.ocr_processor import OCRProcessor

ocr = OCRProcessor(['ru', 'en'])

# Улучшение текста с OCR
enhanced_text, ocr_info = ocr.enhance_pdf_text("document.pdf", original_text)

# Определение типа документа
doc_type = ocr.detect_document_type(enhanced_text)
```

## 🏗️ Архитектура

```
PDF → Text Extraction (pdfplumber) → OCR Fallback → Enhanced Parser → Results
```

### Основные компоненты:

- **`OCRProcessor`** - основной OCR модуль для распознавания текста
- **`EnhancedExtractor`** - улучшенный экстрактор с OCR поддержкой
- **`UniversalParser`** - универсальный парсер с интеграцией OCR
- **GUI с OCR панелью** - управление OCR настройками

## 📊 OCR возможности

### 1. **Автоматическое определение качества**
- Анализ длины извлеченного текста
- Проверка наличия ключевых слов
- Валидация структуры таблиц

### 2. **Умный fallback**
- OCR включается только при низком качестве
- Гибридный режим (стандартный + OCR)
- Оптимизация производительности

### 3. **Анализ документов**
- Определение типа документа
- Валидация извлеченных данных
- Рекомендации по улучшению

## 🧪 Тестирование

### Тест OCR функциональности
```bash
python test_ocr.py
```

### Тест GUI с OCR
```bash
python test_gui_ocr.py
```

## 📈 Производительность

### Время обработки:
- **Стандартный режим**: ~1-2 сек на страницу
- **OCR режим**: ~3-5 сек на страницу
- **Гибридный режим**: ~2-3 сек на страницу

### Память:
- **EasyOCR**: ~500MB при инициализации
- **PyMuPDF**: ~100MB на документ
- **OpenCV**: ~50MB

## 🎛️ GUI функции

### OCR панель управления:
- ✅ Включение/выключение OCR
- 🌐 Выбор языков (русский/английский)
- 📊 Статус OCR
- 🧪 Тестирование OCR функциональности
- 📈 Отчеты о качестве извлечения

### Автоматическая обработка:
- Drag & Drop PDF файлов
- Автоматическое определение необходимости OCR
- Отображение результатов с OCR информацией
- Экспорт результатов в Excel

## 🔍 Примеры использования

### Пример 1: Улучшение качества текста
```python
# Если стандартное извлечение дало плохой результат
if len(text) < 100:
    enhanced_text, ocr_info = ocr.enhance_pdf_text(pdf_path, text)
    print(f"OCR добавил {ocr_info['ocr_additions']} текстовых блоков")
```

### Пример 2: Определение типа документа
```python
doc_type = ocr.detect_document_type(text)
if doc_type['invoice'] > 0.7:
    print("Это счет-фактура")
elif doc_type['commercial_proposal'] > 0.7:
    print("Это коммерческое предложение")
```

### Пример 3: Валидация данных
```python
validation = ocr.validate_extracted_data(text, tables)
if validation['overall_quality'] < 0.6:
    print("Качество низкое, рекомендуется OCR")
    for issue in validation['issues']:
        print(f"- {issue}")
```

## 🚨 Устранение неполадок

### Проблема: OCR не инициализируется
**Решение:**
```bash
pip install easyocr opencv-python pymupdf
```

### Проблема: Медленная работа
**Решение:**
- Отключите OCR для простых документов
- Используйте `enable_ocr_fallback=False`
- Оптимизируйте размер изображений

### Проблема: Плохое качество распознавания
**Решение:**
- Проверьте качество исходного PDF
- Настройте языки OCR
- Увеличьте порог уверенности

## 🔮 Планы развития

### Версия 2.0:
- [ ] Поддержка GPU для ускорения OCR
- [ ] Дополнительные языки
- [ ] Улучшенная предобработка изображений
- [ ] Машинное обучение для классификации

### Версия 2.1:
- [ ] Веб-интерфейс для настройки OCR
- [ ] Пакетная обработка документов
- [ ] Кэширование OCR результатов
- [ ] API для внешних систем

## 📚 Дополнительные ресурсы

- [OCR документация](OCR_README.md) - подробное описание OCR функциональности
- [EasyOCR Documentation](https://github.com/JaidedAI/EasyOCR)
- [PyMuPDF Documentation](https://pymupdf.readthedocs.io/)
- [OpenCV Documentation](https://docs.opencv.org/)

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Запустите тесты: `python test_ocr.py`
3. Проверьте установку зависимостей
4. Создайте issue в репозитории

---

**OCR модуль значительно улучшает качество парсинга PDF документов! 🎉**

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл LICENSE для подробностей.
